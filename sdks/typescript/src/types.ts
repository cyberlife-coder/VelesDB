/**
 * VelesDB TypeScript SDK - Type Definitions
 *
 * Barrel re-export from the `types/` sub-modules. All existing imports
 * from `'./types'` or `'../types'` continue to work unchanged.
 * @packageDocumentation
 */

export * from './types/index';
