/**
 * REST Backend for VelesDB — Barrel re-export
 * 
 * The implementation has been extracted into focused modules under ./rest/.
 * This file exists for backward compatibility: `import { RestBackend } from './backends/rest'`
 */

export { RestBackend } from './rest/index';
