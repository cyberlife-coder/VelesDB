---
phase: 15-langchain-llamaindex-v15-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - integrations/langchain/src/langchain_velesdb/vectorstore.py
  - integrations/langchain/src/langchain_velesdb/security.py
  - integrations/langchain/src/langchain_velesdb/__init__.py
  - integrations/langchain/tests/test_vectorstore.py
autonomous: true
requirements: [SDK-05]

must_haves:
  truths:
    - "add_texts() accepts optional sparse_vectors parameter and includes sparse vectors in upserted points"
    - "similarity_search() accepts sparse_vector kwarg and delegates to collection.search(vector=..., sparse_vector=...) for hybrid search"
    - "train_pq() method calls self._get_db().train_pq() with correct collection name, m, k, opq params"
    - "stream_insert() method builds points (with optional sparse vectors) and calls collection.stream_insert()"
    - "Existing dense-only callers see identical behavior (all new params are Optional with None defaults)"
  artifacts:
    - path: "integrations/langchain/src/langchain_velesdb/vectorstore.py"
      provides: "VelesDBVectorStore with sparse, PQ, streaming methods"
      contains: "def train_pq"
    - path: "integrations/langchain/src/langchain_velesdb/security.py"
      provides: "validate_sparse_vector function"
      contains: "def validate_sparse_vector"
    - path: "integrations/langchain/tests/test_vectorstore.py"
      provides: "Tests for sparse, PQ, streaming features"
      contains: "test_add_texts_with_sparse"
  key_links:
    - from: "vectorstore.py add_texts()"
      to: "collection.upsert()"
      via: "sparse_vector key in point dict"
      pattern: 'point\["sparse_vector"\]'
    - from: "vectorstore.py train_pq()"
      to: "self._get_db().train_pq()"
      via: "Database-level PQ training call"
      pattern: "_get_db\\(\\)\\.train_pq"
    - from: "vectorstore.py stream_insert()"
      to: "collection.stream_insert()"
      via: "streaming channel insert"
      pattern: "collection\\.stream_insert"
---

<objective>
Add v1.5 feature support (sparse vectors, PQ training, streaming inserts) to the official LangChain `langchain-velesdb` package.

Purpose: Close the SDK-05 gap -- the official pip-installable package must expose the same v1.5 capabilities demonstrated by example scripts, so LangChain users can use hybrid dense+sparse search, PQ training, and streaming inserts through the standard VectorStore interface.

Output: Updated `vectorstore.py` with new methods, updated `security.py` with sparse vector validation, updated `__init__.py` version, and tests covering all new features.
</objective>

<execution_context>
@C:/Users/Nicolas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Nicolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-langchain-llamaindex-v15-parity/15-RESEARCH.md
@integrations/langchain/src/langchain_velesdb/vectorstore.py
@integrations/langchain/src/langchain_velesdb/security.py
@integrations/langchain/src/langchain_velesdb/__init__.py
@integrations/langchain/tests/test_vectorstore.py

<interfaces>
<!-- Python SDK method signatures (from crates/velesdb-python/) that the integration must call -->

collection.upsert(points)
# Each point dict can include: {"id": int, "vector": [...], "payload": {...}, "sparse_vector": {0: 1.5, 3: 0.8}}

collection.search(vector=[...], top_k=10)                          # dense only
collection.search(sparse_vector={0: 1.5}, top_k=10)                # sparse only
collection.search(vector=[...], sparse_vector={0: 1.5}, top_k=10)  # hybrid (auto RRF k=60)

db.train_pq(collection_name, m=8, k=256, opq=False)                # PQ training (on Database, NOT Collection)

collection.stream_insert(points)                                    # streaming insert (takes list of point dicts)

<!-- Existing LangChain vectorstore key method signatures -->

_get_db() -> velesdb.Database
_get_collection(dimension: int) -> velesdb.Collection
_to_point_id(id_str: str) -> int
_generate_auto_id() -> tuple[str, int]
_to_document(result: dict) -> Document
_run_vector_search(query_embedding, k, *, filter=None, ef_search=None, ids_only=False) -> List[dict]

add_texts(texts, metadatas=None, ids=None, **kwargs) -> List[str]
similarity_search(query, k=4, **kwargs) -> List[Document]
similarity_search_with_score(query, k=4, **kwargs) -> List[Tuple[Document, float]]
hybrid_search(query, k=4, bm25_weight=0.5, **kwargs) -> List[Document]  # BM25 hybrid -- DO NOT modify
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sparse vector validation and v1.5 methods to LangChain VectorStore</name>
  <files>integrations/langchain/src/langchain_velesdb/security.py, integrations/langchain/src/langchain_velesdb/vectorstore.py, integrations/langchain/src/langchain_velesdb/__init__.py</files>
  <action>
1. **security.py** -- Add `validate_sparse_vector(sparse_vector: dict)` function:
   - Validate type is dict
   - Validate all keys are int (term IDs)
   - Validate all values are int or float (weights)
   - Validate reasonable size limit (e.g., max 100_000 entries)
   - Add `MAX_SPARSE_VECTOR_SIZE = 100_000` constant
   - Raise SecurityError on validation failure

2. **vectorstore.py** -- Add `sparse_vectors` param to `add_texts()`:
   - Add `sparse_vectors: Optional[List[dict]] = None` parameter after `ids`
   - If `sparse_vectors` is not None, validate each with `validate_sparse_vector()`
   - In the point-building loop: if `sparse_vectors is not None and i < len(sparse_vectors)`, add `point["sparse_vector"] = sparse_vectors[i]`
   - Import `validate_sparse_vector` from security module

3. **vectorstore.py** -- Modify `_run_vector_search()` to support sparse:
   - Add `sparse_vector: Optional[dict] = None` parameter
   - When `sparse_vector` is not None: call `collection.search(vector=query_embedding, sparse_vector=sparse_vector, top_k=k)` -- this handles dense+sparse hybrid automatically via RRF
   - When only `sparse_vector` (no embedding): call `collection.search(sparse_vector=sparse_vector, top_k=k)`
   - Existing dense-only path unchanged when `sparse_vector` is None

4. **vectorstore.py** -- Pass `sparse_vector` kwarg through search methods:
   - In `similarity_search()`: extract `sparse_vector = kwargs.get("sparse_vector")`, pass to `_run_vector_search()`
   - In `similarity_search_with_score()`: same pattern
   - DO NOT modify `hybrid_search()` -- that uses BM25, which is a different concept (per pitfall 2 from research)

5. **vectorstore.py** -- Add `train_pq()` method on VelesDBVectorStore:
   ```python
   def train_pq(self, m: int = 8, k: int = 256, opq: bool = False) -> str:
       """Train Product Quantization on the collection.

       PQ training is a Database-level operation (not Collection-level).

       Args:
           m: Number of subspaces. Defaults to 8.
           k: Number of centroids per subspace. Defaults to 256.
           opq: Enable Optimized PQ pre-rotation. Defaults to False.

       Returns:
           Training result message.
       """
       return self._get_db().train_pq(self._collection_name, m=m, k=k, opq=opq)
   ```

6. **vectorstore.py** -- Add `stream_insert()` method:
   ```python
   def stream_insert(
       self,
       texts: Iterable[str],
       metadatas: Optional[List[dict]] = None,
       sparse_vectors: Optional[List[dict]] = None,
       ids: Optional[List[str]] = None,
       **kwargs: Any,
   ) -> int:
       """Insert texts via streaming channel with backpressure.

       Args:
           texts: Texts to insert.
           metadatas: Optional metadata dicts.
           sparse_vectors: Optional sparse vector dicts.
           ids: Optional string IDs.

       Returns:
           Number of points inserted.
       """
   ```
   Implementation: Convert texts to embeddings, build points list (same pattern as `add_texts` including sparse_vector attachment), then call `collection.stream_insert(points)`. Use `validate_batch_size`, `validate_text`, and `validate_sparse_vector` as in `add_texts`.

7. **__init__.py** -- Update `__version__` from `"0.8.10"` to `"1.5.0"` (per pitfall 6 from research).

Do NOT modify the existing `hybrid_search()` method which uses BM25 -- sparse vector hybrid is accessed through the standard `similarity_search()` with `sparse_vector` kwarg.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('integrations/langchain/src/langchain_velesdb/vectorstore.py').read()); ast.parse(open('integrations/langchain/src/langchain_velesdb/security.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>
    - add_texts() accepts sparse_vectors parameter
    - similarity_search/similarity_search_with_score pass sparse_vector kwarg through
    - train_pq() method exists and calls self._get_db().train_pq()
    - stream_insert() method exists and calls collection.stream_insert()
    - validate_sparse_vector() exists in security.py
    - __version__ is "1.5.0"
    - All files pass Python syntax check
  </done>
</task>

<task type="auto">
  <name>Task 2: Add v1.5 feature tests to LangChain test suite</name>
  <files>integrations/langchain/tests/test_vectorstore.py</files>
  <action>
Add test cases to the existing `test_vectorstore.py` file. Use the existing `FakeEmbeddings` class and `temp_db_path` fixture already in the file.

Add a new test class `TestV15Features` (or add methods to the existing `TestVelesDBVectorStore` class) with these test cases:

1. **test_add_texts_with_sparse_vectors** -- Create a VelesDBVectorStore, call `add_texts(["hello"], sparse_vectors=[{0: 1.5, 3: 0.8}])`, verify it returns an ID list (no crash).

2. **test_add_texts_without_sparse_preserves_behavior** -- Call `add_texts(["hello"])` without sparse_vectors, verify same behavior as before (backward compat).

3. **test_similarity_search_with_sparse_vector_kwarg** -- Call `similarity_search("test", sparse_vector={0: 1.0})`, verify it runs without error. The test uses the real VelesDB engine so results depend on data.

4. **test_train_pq_method_exists** -- Verify `hasattr(vectorstore, 'train_pq')` is True. Optionally call it (may fail if collection has too few vectors for PQ training, which is acceptable -- the test validates the method exists and calls the right path).

5. **test_stream_insert_method_exists** -- Verify `hasattr(vectorstore, 'stream_insert')` is True.

6. **test_validate_sparse_vector_valid** -- Import `validate_sparse_vector` from security, call with valid dict `{0: 1.5, 3: 0.8}`, verify no exception.

7. **test_validate_sparse_vector_invalid_type** -- Call `validate_sparse_vector("not_a_dict")`, verify SecurityError is raised.

8. **test_validate_sparse_vector_invalid_keys** -- Call `validate_sparse_vector({"a": 1.0})`, verify SecurityError is raised (keys must be int).

9. **test_version_is_1_5_0** -- `from langchain_velesdb import __version__; assert __version__ == "1.5.0"`.

All tests should follow the existing pattern in the file: use `temp_db_path` fixture, `embeddings` fixture, and create VelesDBVectorStore instances in the same way.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('integrations/langchain/tests/test_vectorstore.py').read()); print('Test file syntax OK')"</automated>
  </verify>
  <done>
    - At least 9 new test methods covering sparse add, sparse search, train_pq, stream_insert, validation, and version
    - Test file passes syntax check
    - Tests follow existing patterns (FakeEmbeddings, temp_db_path fixture)
  </done>
</task>

</tasks>

<verification>
1. All Python files pass syntax check: `python -c "import ast; ast.parse(open(f).read())"` for each modified file
2. `grep -c "def train_pq" integrations/langchain/src/langchain_velesdb/vectorstore.py` returns 1
3. `grep -c "def stream_insert" integrations/langchain/src/langchain_velesdb/vectorstore.py` returns 1
4. `grep -c "def validate_sparse_vector" integrations/langchain/src/langchain_velesdb/security.py` returns 1
5. `grep -c "sparse_vectors" integrations/langchain/src/langchain_velesdb/vectorstore.py` returns >= 5 (param declarations + usage)
</verification>

<success_criteria>
- LangChain VelesDBVectorStore supports sparse_vectors in add_texts()
- Hybrid dense+sparse search works via similarity_search(sparse_vector=...) kwarg
- train_pq() and stream_insert() methods exist and call the correct Python SDK methods
- Existing BM25 hybrid_search() is untouched
- All new parameters are Optional with None defaults (backward compatible)
- Test suite covers all new v1.5 features
- __version__ is "1.5.0"
</success_criteria>

<output>
After completion, create `.planning/phases/15-langchain-llamaindex-v15-parity/15-01-SUMMARY.md`
</output>
