---
phase: 15-langchain-llamaindex-v15-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - integrations/llamaindex/src/llamaindex_velesdb/vectorstore.py
  - integrations/llamaindex/src/llamaindex_velesdb/security.py
  - integrations/llamaindex/src/llamaindex_velesdb/__init__.py
  - integrations/llamaindex/tests/test_vectorstore.py
autonomous: true
requirements: [SDK-06]

must_haves:
  truths:
    - "add() accepts sparse_vectors via add_kwargs and includes them in upserted points"
    - "query() accepts sparse_vector kwarg and delegates to collection.search(vector=..., sparse_vector=...) for hybrid search"
    - "train_pq() method calls self._get_db().train_pq() with correct collection name, m, k, opq params"
    - "stream_insert() method builds points from nodes and calls collection.stream_insert()"
    - "Existing dense-only callers see identical behavior (all new params come via kwargs with None defaults)"
  artifacts:
    - path: "integrations/llamaindex/src/llamaindex_velesdb/vectorstore.py"
      provides: "VelesDBVectorStore with sparse, PQ, streaming methods"
      contains: "def train_pq"
    - path: "integrations/llamaindex/src/llamaindex_velesdb/security.py"
      provides: "validate_sparse_vector function"
      contains: "def validate_sparse_vector"
    - path: "integrations/llamaindex/tests/test_vectorstore.py"
      provides: "Tests for sparse, PQ, streaming features"
      contains: "test_add_with_sparse"
  key_links:
    - from: "vectorstore.py add()"
      to: "collection.upsert()"
      via: "sparse_vector key in point dict from add_kwargs"
      pattern: 'point\["sparse_vector"\]'
    - from: "vectorstore.py train_pq()"
      to: "self._get_db().train_pq()"
      via: "Database-level PQ training call"
      pattern: "_get_db\\(\\)\\.train_pq"
    - from: "vectorstore.py stream_insert()"
      to: "collection.stream_insert()"
      via: "streaming channel insert"
      pattern: "collection\\.stream_insert"
---

<objective>
Add v1.5 feature support (sparse vectors, PQ training, streaming inserts) to the official LlamaIndex `llama-index-vector-stores-velesdb` package.

Purpose: Close the SDK-06 gap -- the official pip-installable package must expose sparse vectors, PQ config, and streaming inserts through the standard BasePydanticVectorStore interface, matching the v1.5 core API surface.

Output: Updated `vectorstore.py` with new methods, updated `security.py` with sparse vector validation, updated `__init__.py` version, and tests covering all new features.
</objective>

<execution_context>
@C:/Users/Nicolas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Nicolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-langchain-llamaindex-v15-parity/15-RESEARCH.md
@integrations/llamaindex/src/llamaindex_velesdb/vectorstore.py
@integrations/llamaindex/src/llamaindex_velesdb/security.py
@integrations/llamaindex/src/llamaindex_velesdb/__init__.py
@integrations/llamaindex/tests/test_vectorstore.py

<interfaces>
<!-- Python SDK method signatures (from crates/velesdb-python/) that the integration must call -->

collection.upsert(points)
# Each point dict can include: {"id": int, "vector": [...], "payload": {...}, "sparse_vector": {0: 1.5, 3: 0.8}}

collection.search(vector=[...], top_k=10)                          # dense only
collection.search(sparse_vector={0: 1.5}, top_k=10)                # sparse only
collection.search(vector=[...], sparse_vector={0: 1.5}, top_k=10)  # hybrid (auto RRF k=60)

db.train_pq(collection_name, m=8, k=256, opq=False)                # PQ training (on Database, NOT Collection)

collection.stream_insert(points)                                    # streaming insert (takes list of point dicts)

<!-- Existing LlamaIndex vectorstore key method signatures -->

_get_db() -> velesdb.Database
_get_collection(dimension: int) -> velesdb.Collection
_build_query_result(results: list[dict]) -> VectorStoreQueryResult

add(nodes: List[BaseNode], **add_kwargs) -> List[str]
query(query: VectorStoreQuery, **kwargs) -> VectorStoreQueryResult
hybrid_query(query: VectorStoreQuery, bm25_weight=0.5, **kwargs)  # BM25 hybrid -- DO NOT modify

<!-- LlamaIndex Pydantic constraint (pitfall 3 from research) -->
# BasePydanticVectorStore uses Pydantic v2 models
# Use private attributes (prefixed with _) for mutable state
# model_config = ConfigDict(arbitrary_types_allowed=True) already set
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sparse vector validation and v1.5 methods to LlamaIndex VectorStore</name>
  <files>integrations/llamaindex/src/llamaindex_velesdb/security.py, integrations/llamaindex/src/llamaindex_velesdb/vectorstore.py, integrations/llamaindex/src/llamaindex_velesdb/__init__.py</files>
  <action>
1. **security.py** -- Add `validate_sparse_vector(sparse_vector: dict)` function (identical to LangChain version):
   - Validate type is dict
   - Validate all keys are int (term IDs)
   - Validate all values are int or float (weights)
   - Add `MAX_SPARSE_VECTOR_SIZE = 100_000` constant
   - Raise SecurityError on validation failure

2. **vectorstore.py** -- Modify `add()` to support sparse vectors via add_kwargs:
   - Extract `sparse_vectors = add_kwargs.get("sparse_vectors")` at the top of `add()`
   - If sparse_vectors is not None, validate each with `validate_sparse_vector()`
   - In the point-building loop: if `sparse_vectors is not None and idx < len(sparse_vectors)` (use enumerate index), add `point["sparse_vector"] = sparse_vectors[idx]`
   - Import `validate_sparse_vector` from security module

3. **vectorstore.py** -- Modify `query()` to support sparse vector via kwargs:
   - Extract `sparse_vector = kwargs.get("sparse_vector")` at the top of `query()`
   - If `sparse_vector` is not None, validate with `validate_sparse_vector()`
   - When `sparse_vector` is provided: call `collection.search(vector=query.query_embedding, sparse_vector=sparse_vector, top_k=k)` instead of the current dense-only call
   - When both `sparse_vector` and `query.query_embedding` are provided, the SDK handles RRF fusion automatically
   - Existing dense-only path unchanged when `sparse_vector` is None
   - Keep the existing MetadataFilters path working (filter + sparse can coexist if both provided -- apply sparse to the search call, filter still works)

4. **vectorstore.py** -- Add `train_pq()` method:
   ```python
   def train_pq(self, m: int = 8, k: int = 256, opq: bool = False) -> str:
       """Train Product Quantization on the collection.

       PQ training is a Database-level operation (not Collection-level)
       because TRAIN QUANTIZER requires Database-level VelesQL execution.

       Args:
           m: Number of subspaces. Defaults to 8.
           k: Number of centroids per subspace. Defaults to 256.
           opq: Enable Optimized PQ pre-rotation. Defaults to False.

       Returns:
           Training result message.
       """
       return self._get_db().train_pq(self.collection_name, m=m, k=k, opq=opq)
   ```

5. **vectorstore.py** -- Add `stream_insert()` method:
   ```python
   def stream_insert(
       self,
       nodes: List[BaseNode],
       **kwargs: Any,
   ) -> int:
       """Insert nodes via streaming channel with backpressure.

       Args:
           nodes: List of nodes with embeddings to insert.
           **kwargs: Additional arguments. Supports 'sparse_vectors' list.

       Returns:
           Number of points inserted.
       """
   ```
   Implementation: Extract embeddings from nodes, build points list (same pattern as `add()` including sparse_vector from `kwargs.get("sparse_vectors")`), then call `collection.stream_insert(points)`. Use `validate_batch_size` for node count.

6. **__init__.py** -- Update `__version__` from `"0.8.10"` to `"1.5.0"` (per pitfall 6 from research).

Do NOT modify the existing `hybrid_query()` method which uses BM25 -- sparse vector hybrid is accessed through the standard `query()` with `sparse_vector` kwarg.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('integrations/llamaindex/src/llamaindex_velesdb/vectorstore.py').read()); ast.parse(open('integrations/llamaindex/src/llamaindex_velesdb/security.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>
    - add() extracts sparse_vectors from add_kwargs and attaches to points
    - query() extracts sparse_vector from kwargs and passes to collection.search()
    - train_pq() method exists and calls self._get_db().train_pq()
    - stream_insert() method exists and calls collection.stream_insert()
    - validate_sparse_vector() exists in security.py
    - __version__ is "1.5.0"
    - All files pass Python syntax check
  </done>
</task>

<task type="auto">
  <name>Task 2: Add v1.5 feature tests to LlamaIndex test suite</name>
  <files>integrations/llamaindex/tests/test_vectorstore.py</files>
  <action>
Add test cases to the existing `test_vectorstore.py` file. Use the existing `TestVelesDBVectorStore` class, `temp_dir` fixture, and `vector_store` fixture already in the file.

Add these test methods (in the existing class or a new `TestV15Features` class):

1. **test_add_with_sparse_vectors** -- Create TextNodes with embeddings, call `vector_store.add(nodes, sparse_vectors=[{0: 1.5, 3: 0.8}])`, verify it returns node IDs (no crash).

2. **test_add_without_sparse_preserves_behavior** -- Call `vector_store.add(nodes)` without sparse_vectors kwarg, verify same behavior as before (backward compat).

3. **test_query_with_sparse_vector_kwarg** -- Create a VectorStoreQuery with query_embedding, call `vector_store.query(query, sparse_vector={0: 1.0})`, verify it returns a VectorStoreQueryResult (no crash).

4. **test_train_pq_method_exists** -- Verify `hasattr(vector_store, 'train_pq')` is True.

5. **test_stream_insert_method_exists** -- Verify `hasattr(vector_store, 'stream_insert')` is True.

6. **test_validate_sparse_vector_valid** -- Import `validate_sparse_vector` from security, call with `{0: 1.5, 3: 0.8}`, verify no exception.

7. **test_validate_sparse_vector_invalid_type** -- Call `validate_sparse_vector("not_a_dict")`, verify SecurityError is raised.

8. **test_validate_sparse_vector_invalid_keys** -- Call `validate_sparse_vector({"a": 1.0})`, verify SecurityError is raised.

9. **test_version_is_1_5_0** -- `from llamaindex_velesdb import __version__; assert __version__ == "1.5.0"`.

Use the existing TextNode pattern from the file (e.g., `TextNode(text="Hello", id_="node1", embedding=[0.1] * 768)`).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('integrations/llamaindex/tests/test_vectorstore.py').read()); print('Test file syntax OK')"</automated>
  </verify>
  <done>
    - At least 9 new test methods covering sparse add, sparse query, train_pq, stream_insert, validation, and version
    - Test file passes syntax check
    - Tests follow existing patterns (TextNode, temp_dir fixture)
  </done>
</task>

</tasks>

<verification>
1. All Python files pass syntax check: `python -c "import ast; ast.parse(open(f).read())"` for each modified file
2. `grep -c "def train_pq" integrations/llamaindex/src/llamaindex_velesdb/vectorstore.py` returns 1
3. `grep -c "def stream_insert" integrations/llamaindex/src/llamaindex_velesdb/vectorstore.py` returns 1
4. `grep -c "def validate_sparse_vector" integrations/llamaindex/src/llamaindex_velesdb/security.py` returns 1
5. `grep -c "sparse_vector" integrations/llamaindex/src/llamaindex_velesdb/vectorstore.py` returns >= 5
</verification>

<success_criteria>
- LlamaIndex VelesDBVectorStore supports sparse_vectors in add() via add_kwargs
- Hybrid dense+sparse search works via query(sparse_vector=...) kwarg
- train_pq() and stream_insert() methods exist and call the correct Python SDK methods
- Existing BM25 hybrid_query() is untouched
- All new parameters come via kwargs (backward compatible -- no signature changes)
- Test suite covers all new v1.5 features
- __version__ is "1.5.0"
</success_criteria>

<output>
After completion, create `.planning/phases/15-langchain-llamaindex-v15-parity/15-02-SUMMARY.md`
</output>
